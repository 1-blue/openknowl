import styled from 'styled-components';

const StyledNav = styled.nav``;

/** 2023/09/18 - `<footer>` page component - by 1-blue */
const Nav: React.FC = () => {
  return <StyledNav></StyledNav>;
};

export default Nav;
