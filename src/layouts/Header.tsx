import styled from 'styled-components';

const StyledHeader = styled.header``;

/** 2023/09/18 - `<header>` layout component - by 1-blue */
const Header: React.FC = () => {
  return <StyledHeader></StyledHeader>;
};

export default Header;
